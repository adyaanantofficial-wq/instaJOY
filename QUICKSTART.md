# Quickstart

1. Copy `.env.example` to `.env`
2. Fill in `MONGODB_URI`, `JWT_SECRET`, and `JWT_REFRESH_SECRET`
3. Run `yarn install`
4. Run `yarn start`
5. Read [DEPLOYMENT.md](DEPLOYMENT.md) before pushing to Render or GitHub Pages
